/**
 * AI configuration constants that can be safely imported in client components.
 * Keep this in sync with the defaults in ai.ts.
 */
export const AI_CONSTANTS = {
  dailyMessageLimit: 50,
} as const
